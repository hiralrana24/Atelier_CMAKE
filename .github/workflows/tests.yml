  Test_Add:
    runs-on: ubuntu-latest
    needs: Compilation
    steps:
      - name: Test fonction add
        run: |
          last_directory=$(basename ${{ runner.workspace }})
          git clone https://github.com/${{ github.repository }}.git
          cd ./$last_directory
          mkdir build
          cd build
          cmake ..
          make
          ./src/calculator add 10 5

Test_Car:
  runs-on: ubuntu-latest
  needs: Compilation
  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Build project
      run: |
        mkdir build
        cd build
        cmake ..
        make

    - name: Run CAR test
      run: |
        RESULT=$(./build/src/calculator car 5)
        echo "Résultat du test car : $RESULT" > rapport_car.txt

    - name: Upload rapport CAR
      uses: actions/upload-artifact@v4
      with:
        name: rapport_car
        path: rapport_car.txt
